name: Hello World Workflow  # Название workflow
on: [push]                 # Запускать при каждом push в репозиторий

jobs:                      # Список задач
  say-hello:               # Название задачи (можешь изменить)
    runs-on: ubuntu-latest # ОС для запуска (бесплатный сервер от GitHub)
    steps:                 # Шаги задачи
      - name: Print Hello  # Название шага
        run: echo "🚀 Привет, GitHub Actions!"  # Команда для выполнения